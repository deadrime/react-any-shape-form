import { Meta, Primary, Controls, Story, ArgTypes, Source, Title } from '@storybook/addon-docs/blocks';

import * as FormStories from '../stories/Form.stories';
import * as FormItemStories from '../stories/FormItem.stories';
import FormItem from '../stories/formItem.meta.tsx';

<Meta
  title="Welcome"
  parameters={{
    viewMode: 'docs',
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

<Title>React any shape form</Title>

Lightweight form library focused on ease of use. This library was inspired by `antd` form component and `react-hook-form`.

![npm bundle size](https://deno.bundlejs.com/badge?q=react-any-shape-form@latest)

## Features

- Small size (~10kb)
- Type-friendly, all components and hooks fully typed
- Ease to use, usually you need only `createForm` method to get all the functionality
- Build in promise based validation, you can easy use your own promise function to validate fields
- No extra re-renders

## Install
```bash
npm i react-any-shape-form
```

## `Form` props
<ArgTypes of={FormStories} />

## `Form.Item` props
<ArgTypes of={FormItemStories} />
