import { Meta } from "@storybook/react-vite";
import { FormItem } from "../src";

const meta: Meta<typeof FormItem> = {
  component: FormItem,
  parameters: {
    docsOnly: true,
  },
};

export default meta;
